/**
 * Public contracts for Agent Framework for Java.
 *
 * <p>Types in this package are host neutral. They must not depend on an application framework, a
 * dependency injection container, or a model provider SDK, because a host runtime owns those
 * concerns.
 *
 * <p>Requirements for this package live in {@code docs/requirements/01-agent-execution.md} and the
 * sibling requirement documents.
 */
package io.github.hellices.agentframework.api;
